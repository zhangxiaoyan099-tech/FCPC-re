seed: 42

dataset:
  name: cifar10
  num_classes: 10
  input_channels: 3
  root: ./data
  download: false
  augment: false
  normalize: true

model:
  name: resnet18

federated:
  num_clients: 10
  batch_size: 32
  device: cuda:0
  num_workers: 0
  pin_memory: true

partition:
  mode: dual_skew
  alpha: 0.1
  quantity_min_fraction: 0.25
  preserve_all_samples: true

evaluation:
  validation_fraction: 0.1
  validation_seed: 10042

audit:
  checkpoints: [0]
  panels: [raw, ldp]
  epsilon: 1.0
  ldp_seed: 20042
  lambda_jsdn: 0.3
  output_dir: outputs/jsdn_gradient_update_smoke
  gradient_batch_size: 32
  gradient_max_batches: 2
  gradient_num_workers: 0
  gradient_model_mode: train

  warmup:
    batch_size: 32
    local_epochs: 1
    max_batches_per_client: 2
    num_workers: 0
    optimizer:
      name: sgd
      lr: 0.05
      momentum: 0.9
      weight_decay: 0.0005

  update_probe:
    batch_size: 32
    local_epochs: 1
    max_batches_per_client: 2
    num_workers: 0
    optimizer:
      name: sgd
      lr: 0.05
      momentum: 0.9
      weight_decay: 0.0005
